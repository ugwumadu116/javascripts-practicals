/*Write a JavaScript program to test the first character of a string is uppercase or not.*/

function one(str){
return /^[A-Z]/.test(str);
};
console.log(one('aaa'));
console.log(one('RrerA'));


/*function twt(str){
	return str[0]===str[0].toUpperCase()?true:false

}

console.log(twt("oele"))
*/
