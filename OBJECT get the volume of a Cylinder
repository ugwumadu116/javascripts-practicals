/*Write a JavaScript program to get the volume of a Cylinder with four decimal places using object classes.
Volume of a cylinder : V = πr2h
where r is the radius and h is the height of the cylinder.*/


function Cylinder(height, radius){
	this.height = height
	this.radius = radius	
}

Cylinder.prototype = {
	length : function(){return 1},
	legs : function(){return "She Has two Legs"},
	home: "her home is in Owerri",
	volume: function(){return Math.PI * this.height * this.radius}
}


her = new Cylinder(7, 3)
console.log(her.volume().toFixed(3))
console.log(her.length() + her.height)
